{
  "name": "myapp",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev -w packages/backend\" \"npm run dev -w packages/frontend\"",
    "build": "npm run build -w packages/backend && npm run build -w packages/frontend",
    "start": "npm run start:prod -w packages/backend & npm run start:prod -w packages/frontend"
  },
  "devDependencies": {
    "@nestjs/testing": "^11.1.28",
    "@types/jest": "^30.0.0",
    "concurrently": "^9.0.0",
    "jest": "^30.4.2",
    "ts-jest": "^29.4.12"
  },
  "engines": {
    "node": ">=18"
  }
}
